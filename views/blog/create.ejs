<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <link rel="icon" href="/images/ppd_logo_circle.png" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin | Create Blog Post</title>
  <style>
    body {
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      background-color: rgba(226, 223, 223, 0.474);
      margin: 0;
      padding: 0;
      user-select: none;
      -webkit-tap-highlight-color: transparent;
      text-align: center
    }

    .container {
      max-width: 90%;
      height: 100%;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h1 {
      margin-top: 130px;
      color: #333;
    }

    form {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    label {
      margin-bottom: 5px;
      color: #555;
    }

    input,
    textarea {
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      padding: 8px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
      width: 50%;
      outline: none;
      font-size: 14px;
      letter-spacing: 1px;
    }
    textarea{
      resize: vertical;
    }

    button {
      padding: 10px;
      background-color: #4596dd;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background-color: #2980b9;
    }

    .logout-btn, .delete-btn{
      font-weight: 400;
      margin-left: 10px;
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      padding: 10px;
      cursor: pointer;
      background-color: #ff5d5d;
      border: 1px solid #fe5d5d;
      border-radius: 5px;
      transition: 0.2s;
      color: black;
    }
    .logout-btn:hover, .delete-btn:hover{
      background-color: rgba(226, 223, 223, 0.474);
    }
    .create-blog-div{
      display: flex;
      justify-content: center;
      align-items: center;
    }

    @media only screen and (max-width: 468px) {
      input,
    textarea {
      width: 90%;
    }
    }

  </style>



</head>

<body>
  <div class="blog-create-nav">
    <%- include('../navbar/navbar.ejs') %>
  </div>

  <h1>Create a New Blog Post</h1>
  <div class="create-blog-div">
    <a href="/blog/delete"><button type="submit" class="delete-btn">Delete a Blog</button></a>
    <form action="/" method="post">
      <button type="submit" class="logout-btn">Log Out</button>
    </form>
  </div>

  <form action="/blog/create" method="post" class="container">
    <label for="heading">Heading:</label>
    <input type="text" id="heading" name="heading" required>
    <label for="description">Description:</label>
    <textarea id="description" name="description" rows="13" required></textarea>
    <button type="submit">Create Blog Post</button>
  </form>
</body>

</html>